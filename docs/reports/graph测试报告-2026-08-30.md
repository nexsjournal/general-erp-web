# graph 全角度测试报告（2026-08-30）

## 验收结论
**可以交付**。4 路全角度测试（功能/UI 83页+264次按钮点击+业务链、数据 金额守恒/幂等/状态机/并发、安全 越权31项/注入7项/泄露面）+ 独立验证，无 P0；2 个初判 P1 经独立验证降级（1 个=Frappe 上游默认行为，1 个=部署方式问题非应用 bug）；当场修复 3 个已确认 bug 并回归 33/33 全绿（commit 084d4bd 已 push）。

## 测试范围
| 路 | 范围 | 结果 |
|---|---|---|
| 功能+UI（A） | 83 页全扫 + 21 核心页 264 次按钮实测 + 销售链/审批链真人点击 + 中文残留/图标/四态/深层返回 | 全通，0 P0 |
| 数据（B） | 回归 33 项 + 销售/采购链金额守恒 DB 对账 + 幂等双提交 + 状态机 4 例 + 报表口径 3 个对账 + 10 并发×50 请求(P95=91ms) | 账实相符，4×P2 |
| 安全（C） | 越权矩阵 31 项 + 注入 7 项 + 日志/密钥/文件面 + 登录限流 | 0 放行，1×P1(降级)+3×P2 |

## Bug 清单（独立验证后定级）
| # | 级别 | 问题 | 验证结论 | 处置 |
|---|---|---|---|---|
| G1 | ~~P1~~→P2 | sales1 能打开用户管理页看全量用户姓名+邮箱 | 根因=Frappe 上游默认（Desk User 角色在 User 单据带 select 权限，desk 渲染"负责人"下拉必需）；只暴露激活用户姓名+邮箱，无密码/角色/权限，API 层写操作全 403 | 观察项，建卡 D1（生产可收紧） |
| G2 | P1 | 错误响应泄露完整 Python 堆栈+源码路径 | 已复现（/files/ 500 带 /Users/god/... 路径）。根因=frappe serve 开发服务器，**非应用代码 bug**，生产 WSGI 部署即消除 | 部署清单卡 D2 |
| G3 | P2 | 登录无限流（错密码 6 连试无锁定） | 已复现，Frappe 默认未启用 lockout | 卡 D3（生产必配） |
| G4 | P2 | /files/ 500 泄露绝对路径 | 已复现，同 G2 部署根因 | 并入 D2 |
| G5 | P2 | site_config.json 明文密钥 644 | 已复现 | **已修**：chmod 600（bench 本地，不入 git） |
| G6 | P2 | 外销统计 N+1 相关子查询 | 已复现（每行 1 子查询） | **已修**：LEFT JOIN 预聚合，口径守恒验证通过（3 种分组数字全一致） |
| G7 | P2 | 付款并发双击败方收 500 死锁报错 | B 路复现，数据安全（无重复入账）但报错不友好 | 卡 D4（体验优化） |
| G8 | P2 | 重复 submit 返回 200"成功" | Frappe v16 标准行为（update_after_submit 路径），无副作用 | 不修，认知项 |
| G9 | P2 | 发票 paid_amount 全额收款后恒 0 | ERPNext v16 重构行为（outstanding_amount 正确=0） | 不修，前端勿读该字段（已记经验库） |
| G10 | P2 | 客户360"查看"按钮疑似死按钮 | 独立复测：空输入时按设计静默 return；真实客户名→URL 变化+完整渲染 360 视图（截图 /tmp/v360_real.png） | **误报，关闭** |
| G11 | P2 | Report Role 空态无列表 chrome | 空数据 DocType 列表页，属 frappe 正常空态 | 观察，不修 |
| G12 | P2 | 首页"今日汇率"显示 6.718000000（9 位小数） | 已复现，根因 show_full_number=1 + USD/CNY 原始精度 | **已修**：bundle 非侵入补丁强制 2 位（6.72），浏览器验证通过 |

## 狂闪问题（用户报修）
已确认修复生效：/desk/index 加载后 8 秒观察窗口内 0 次二次导航/重载，无 console 报错，首页 4 数字卡+13 模块磁贴完整渲染（截图 /tmp/final_home.png）。此前的防闪补丁（清 session_last_route 脏种子 + set_route 守卫掐断 setup-wizard 循环）有效。

## 已修 commit
084d4bd fix(T-graph-fixes)：外销统计 N+1 / 汇率卡 2 位小数 / 回归脚本 TODAY 动态化。回归 33/33 PASS。

## 遗留任务卡
见 docs/reports/T13-待办-graph测试遗留-P2.md（D1-D4 部署/体验类，均不阻断交付）。

---

## 第二轮全量复测（2026-08-30，4 路并行 + 独立验证）
本轮按 graph-test 技能重新跑全模块 + 业务 + 安全 + UI，并在测试中修复了上一轮遗留的用户报修项（登录后首页落地 + 会话超时）。

| 路 | 范围 | 结果 |
|---|---|---|
| 功能 A | 13 模块逐一点击 + 首页/销售订单深层走查（列表→详情→返回父页状态保留） | 14/14 模块可达、无空白页、深层销售订单链走通、返回不丢态，0 P0 |
| 数据 B | 30+ 单据/主数据列表接口真实返回 + 10 并发×Sales Order P95 | 全 200 有数；10 并发 P95=586ms、10/10 成功，<2s 达标 |
| 安全 C | 越权矩阵（sales1 读 System Settings / 读他人 User / 财务·采购·跨角色单据 / admin 全量） | 角色矩阵正确：sales1 用户列表只见自己、财务/采购/组织单据 403、admin 全量 9 人；无跨角色数据泄露，0 P0/P1 |
| UI D | sales1 视角边界态 + 死按钮 sweep + 图标检查 + ERP工作台可见性 | ERP工作台对 sales1 正确隐藏；死按钮/图标为探针误报，复测无实锤死按钮 |

### 回归
scripts/regression.sh 全量：**44/44 PASS**（chain 2/2 + fin_reports 4/4 + modules 15/15 + permissions 12/12 + approval_guard 7/7）。

### 本轮已修（用户报修项）
- **会话超时**：System Settings.session_expiry 由默认 170:00(≈7天) 固化为 8:00，闲置超 8 小时强制重新输账号密码；site_setup.sync_user_login_settings 幂等固化防 migrate 回滚。commit 7d6d817。
- **登录后首页落地**：实测裸 IP 无会话→跳登录页；登录后落 /desk/index（金蝶式首页，今日概览+13 模块侧栏）。所有系统用户 default_workspace=index，普通用户侧栏不显示 ERP工作台。符合预期。

### 环境项（非代码缺陷，不阻断交付）
- socket.io 走 8002 的 /socket.io/ 轮询 404：本地 bench serve(werkzeug) 未带 socket.io 代理（正式 bench start/gunicorn 部署才有），node socketio 进程在 9002 存活。影响实时通知/在线协作的实时性，核心 CRUD/报表/审批全走 HTTP 不受影响。
- 单测里 3 个 doctype 名我误写（Customs Code→实际 HS Code、Trade Term→Incoterms），数据均在，非缺失。

### 结论
全模块 + 业务 + 安全 + UI 四路 + 44 项回归全绿，**0 P0 / 0 P1**。遗留 P2 仍为上一轮 D1-D4（生产部署类：登录限流/堆栈泄露/密钥权限/付款并发体验），均不阻断交付。
